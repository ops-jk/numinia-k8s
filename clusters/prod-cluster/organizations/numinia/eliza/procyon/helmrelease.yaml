apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: multiplex-pro
  namespace: numinia
  annotations:
    reconcile.fluxcd.io/requestedAt: "1746623178"
spec:
  interval: 5m
  releaseName: eliza-pro
  targetNamespace: numinia
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: ./clusters/prod-cluster/base/eliza
      sourceRef:
        kind: GitRepository
        name: numinia-k8s
        namespace: flux-system
      interval: 1m
  valuesFrom:
    - kind: ConfigMap
      name: multiplex-pro-values
      valuesKey: values.yaml 