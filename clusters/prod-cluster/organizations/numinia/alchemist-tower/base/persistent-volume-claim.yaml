apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hyperfy2-efs-claim
  namespace: numinia
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 5Gi 