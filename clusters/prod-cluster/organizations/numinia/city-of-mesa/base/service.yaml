apiVersion: v1
kind: Service
metadata:
  labels:
    app: city-of-mesa
    pod-type: city-of-mesa
  name: city-of-mesa
  namespace: numinia
spec:
  selector:
    app: city-of-mesa
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000