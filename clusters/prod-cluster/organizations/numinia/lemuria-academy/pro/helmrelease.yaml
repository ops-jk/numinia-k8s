apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: lemuria-academy-pro
  namespace: numinia
  annotations:
    reconcile.fluxcd.io/requestedAt: "1745499146"
spec:
  interval: 5m
  releaseName: lemuria-academy-pro
  targetNamespace: numinia
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: ./clusters/prod-cluster/base/hyperfy2
      sourceRef:
        kind: GitRepository
        name: numinia-k8s
        namespace: flux-system
      interval: 1m
  valuesFrom:
    - kind: ConfigMap
      name: lemuria-academy-pro-values
      valuesKey: values.yaml 