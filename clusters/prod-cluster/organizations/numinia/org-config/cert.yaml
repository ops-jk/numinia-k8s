apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: numinia-tls
  namespace: numinia
spec:
  secretName: numinia-tls
  dnsNames:
    - "numinia.xyz"
    - "pre.numinia.xyz"
  issuerRef:
    name: letsencrypt-numinia-prod
    kind: ClusterIssuer