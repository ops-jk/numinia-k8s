apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: r3s3t-pro
  namespace: numen-games
  annotations:
    reconcile.fluxcd.io/requestedAt: "1748883957"
spec:
  interval: 5m
  releaseName: r3s3t-pro
  targetNamespace: numen-games
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: ./clusters/prod-cluster/base/hyperfy2
      sourceRef:
        kind: GitRepository
        name: numinia-k8s
        namespace: flux-system
      interval: 1m
  valuesFrom:
    - kind: ConfigMap
      name: r3s3t-pro-values
      valuesKey: values.yaml 