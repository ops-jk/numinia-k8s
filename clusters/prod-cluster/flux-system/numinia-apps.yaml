apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: numinia-apps
  namespace: flux-system
spec:
  interval: 10m0s
  path: "./clusters/prod-cluster/apps/numinia/apps"
  prune: true
  sourceRef:
    kind: GitRepository
    name: mi-repositorio   # Asegúrate de que este sea el nombre de tu fuente Git configurada
  validation: client
