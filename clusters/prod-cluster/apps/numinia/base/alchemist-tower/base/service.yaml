apiVersion: v1
kind: Service
metadata:
  name: "${WORLD_NAME}"
  namespace: numinia
spec:
  selector:
    app: "${WORLD_NAME}"
  ports:
    - protocol: TCP
      port: "${PORT}"
      targetPort: "${PORT}"