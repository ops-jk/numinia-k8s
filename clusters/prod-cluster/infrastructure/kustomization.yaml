apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # 1. CSI Drivers y operadores base
  - aws-efs-csi-driver
  - secrets-store-csi-driver
  - cert-manager
  - keda
  # 2. Configuración de DNS y certificados
  - acme-dns
  # 3. Servicios que dependen de certificados
  - traefik 