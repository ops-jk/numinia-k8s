apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: acme-dns-api
  namespace: cert-manager
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`auth.numinia.xyz`)
      kind: Rule
      services:
        - name: acme-dns-api
          port: 80 