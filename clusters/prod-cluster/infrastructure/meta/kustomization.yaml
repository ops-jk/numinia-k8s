apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# This file is intentionally empty.
# It is used as a target for a "meta" kustomization 
# that only exists to group other kustomizations via dependsOn. 