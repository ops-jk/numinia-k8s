apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: acme-dns-storage
  namespace: cert-manager
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: efs-sc 